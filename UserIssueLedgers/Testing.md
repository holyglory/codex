# User Issue Ledger: Testing

| ID | Applies to | Mistake pattern | Required behavior | Prevention and verification |
| --- | --- | --- | --- | --- |
| UIL-TESTING-001 | `/home/CodexMulti` test, build, schema, runtime, and handoff workflows | Continuing to invoke DevCoordinator for this repository after it duplicated resource-heavy runs and the user withdrew and later reaffirmed withdrawal of its use | Do not invoke DevCoordinator for this repository unless the user explicitly requests it for a concrete operation | Before orchestration, read this ledger and use repository-local commands only; report local environment limitations honestly and verify that no Coordinator plan, submission, follow, retry, cancellation, deployment, or runtime action is performed |
| UIL-TESTING-002 | Candidate builds and test infrastructure changes | Launching full validation repeatedly before focused local checks establish that changed behavior and its consumers work; a combined filter can silently miss a renamed test family | Clear focused producer and consumer checks and storage/tool prerequisites before starting any full test rig; require each selected test family to run tests; preserve useful compiler caches and freeze the repair batch before a final complete pass | Make the candidate preflight a success dependency of full Rust, Bazel and native builds; fail zero-test selections separately for each family; record exact-source focused results and cache hit/miss evidence; distinguish executed tests from sandbox early returns and retry unchanged-source transient failures without dispatching the complete matrix |
| UIL-TESTING-003 | Compiler-cache integration in long release builds | Proving only tiny cold and warm compilations misses cache-server inactivity shutdown during a single long compilation, causing fallback recompilation and release timeouts | Keep the cache server alive through long individual compilations, without removing the job's execution limit; do not claim a speed improvement from setup success alone | Reproduce inactivity shutdown with the pinned cache binary and a bounded slow compiler; verify the repaired configuration completes without fallback, reuses the result, and invalidates it when source changes before another full candidate run |
