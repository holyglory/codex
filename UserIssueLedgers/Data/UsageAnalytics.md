# User Issue Ledger: Data / usage analytics

| ID | Applies to | Mistake pattern | Required behavior | Prevention and verification |
| --- | --- | --- | --- | --- |
| UIL-DATA-USAGE-ANALYTICS-001 | Activity classification of provider tokens across root, delegated, same-turn, and follow-up model requests | Keying agent-declared activity only by one thread-and-turn pair lets the classified control/tool operation diverge from the later model request that owns provider token observations; reports then show measured tokens as Unknown while declared activities have zero tokens | Carry a declaration only across factual request lineage: the declaring agent's next request may consume it across a turn change, and a delegated child may inherit the applicable parent declaration unless the child explicitly declares its own; never infer from timing, rewrite historical rows, move token ownership, duplicate totals, or hide genuinely undeclared work | Cover same-turn and next-turn activation, parent-to-child inheritance, child override, classified operations without tokens, token-bearing unknown operations, exact total-token invariance, truthful partial coverage, privacy exclusions, and parity between native repository summaries and the downstream aggregate |
