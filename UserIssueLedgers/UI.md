# User Issue Ledger: UI

| ID | Applies to | Mistake pattern | Required behavior | Prevention and verification |
| --- | --- | --- | --- | --- |
| UIL-UI-001 | Usage requests and usage-based agent self-analysis in the ChatGPT desktop/Codex chat surface and the Codex TUI | Treating local usage as an official-documentation question, or describing a TUI-only slash-command interception as universal, makes model-routed agents overlook the built-in collector and answer without the requested evidence | When `usage_stats` is available, recognize it for natural-language analysis or self-reflection about usage, activity, tools, tokens, or task history as well as model-routed `/usage` requests; call it before answering and consult OpenAI documentation only when the user separately asks about official product behavior, while the TUI may still intercept `/usage ...` locally | Exercise natural-language usage reflection and `/usage all` through supported rendered surfaces; assert that model-routed agents receive concise developer guidance and invoke `usage_stats`, that the TUI opens its local usage view, and that each path returns a bounded truthful result or concise user-facing failure without internal ledger IDs |
